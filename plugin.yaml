apiVersion: argoproj.io/v1alpha1
kind: ConfigManagementPlugin
metadata:
  name: kustomize-env-plugin
spec:
  init:
    command: [sh]
    args: [-c, 'echo "Initializing kustomize-env-plugin"']
  discover:
    find:
      command: ["echo", "hi"]
  generate:
    command: ["render-kustomize.sh"]